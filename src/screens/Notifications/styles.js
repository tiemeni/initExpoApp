import colors from "../../constants/colours";

const styles = {
    container: {
        padding: 20
    },

    headerTitle: {
        fontSize: 22,
        fontWeight: '500'
    },

    groupTitle: {
        fontSize: 18,
        color: colors.text_grey_hint
    },
}

export default styles;