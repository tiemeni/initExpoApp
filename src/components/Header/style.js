import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    constainer: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
        padding: 10,
        height:55
    },
    title: {
        fontSize: 20,
        fontWeight:"600"
    }
})